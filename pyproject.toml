[build-system]
requires = ["setuptools>=42", "wheel", "pybind11>=2.9.0", "pyarrow>=7.0.0"]
build-backend = "setuptools.build_meta"

[tool.isort]
profile = "black"
known_first_party = ["_cassarow"]

[tool.black]
target-version = ['py39']

[mypy]
python_version = 3.9

